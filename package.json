{
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2.5.8"
  },
  "scripts": {
    "prepare": "husky",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "develop": "docker-compose up --build --watch",
    "seed": "pnpm --filter back-end seed:docker"
  },
  "packageManager": "pnpm@10.19.0"
}
